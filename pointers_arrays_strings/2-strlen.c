#include "main.h"
#include <string.h>

/**
 * _strlen - Short description
 *
 * Return: Always 0 (Success)
 * @s: Variable
 */

int _strlen(char *s)
{

int len;

len = _strlen(char *s);


return (0);
}
