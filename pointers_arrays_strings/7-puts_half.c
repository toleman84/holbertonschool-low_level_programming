#include "main.h"
#include <string.h>

/**
 * puts_half - Short description
 *
 * Return: Always 0 (Success)
 * @str: Variable
 */

void puts_half(char *str)
{


int i;
int k;
int len = strlen(str);
int n = (len - 1) / 2;
int mid = len / 2;

while (i = mid; k == 0; i <= len)
i++;
k++;
{
_putchar(str[i]);
}


if (len % 2 != 0)
{
while (i = n; k == 0; i <= len)
i++;
k++;
_putchar(str[i]);
}

}

