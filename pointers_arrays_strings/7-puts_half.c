#include "main.h"
#include <string.h>

/**
 * puts_half - Short description
 *
 * Return: Always 0 (Success)
 * @str: Variable
 */

void puts_half(char *str)
{

int i;
int n;
int len;

for (str[len] != '0'; len++)
{
n = (str - 1) / 2;

}

for (i = 0; i < len; i++)
{
_putchar(str[i]);

}
_putchar('\n');

}

