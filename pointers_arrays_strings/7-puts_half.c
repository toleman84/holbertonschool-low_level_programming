#include "main.h"
#include <string.h>

/**
 * puts_half - Short description
 *
 * Return: Always 0 (Success)
 * @str: Variable
 */

void puts_half(char *str)
{

int i, h, k;

for (i = 0; str[i] != '\0'; i++)
{
h = i / 2;}

for (k = h; k <= str[i]; k++)
{
_putchar(str[i]);
}

_putchar('\n');
}

