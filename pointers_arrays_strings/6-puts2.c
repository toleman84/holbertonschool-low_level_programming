#include "main.h"
#include <string.h>

/**
 * puts2 - Short description
 *
 * Return: 0 (Success)
 * @str: Variable
 */

void puts2(char *str)
{

int i = 0;
int len = strlen(str);

for (; i < len; i += 0)
{
_putchar(str[i]);

}

}
