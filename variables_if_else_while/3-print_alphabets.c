#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Short description
 *
 * Return: 0 (Success)
 */

int main(void)
{

char abc;

while((abc = 'a'; abc <= 'z'; abc++))
{
putchar(abc);
}
while((abc = 'A'; abc <= 'Z'; abc++))
{
putchar(abc);
}
putchar('\n');
return (0);
}

