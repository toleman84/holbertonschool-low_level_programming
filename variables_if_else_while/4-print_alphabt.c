#include <stdio.h>

/**
 * main - Short description
 *
 * Return: 0 (Success)
 */

int main(void)
{

char abc;

for (abc = 'a'; abc <= 'd'; abc = 'f'; abc <= 'p'; abc = 'r'; abc <= 'z'; abc++)
{
putchar(ch);
ch++;
}
putchar('\n');
return (0);
}

