#include "main.h"

/**
 * _isdigit - Short description
 *
 * Return: Always 0 (Success)
 * @c: Variable
 */

int _isdigit(int c)
{

int num;

for (num = '0'; num <= '9'; num++)
{
{
if (c == num)
}
return (1);
}

return (0);
}

